extends ../layout.pug

block content
    form.form.center-form(method='post', action='/genre/add')
        h2 Add New Genre
        .form-group
            label(for='name') Name
            input#name.input-field(name='name', type='text')
        .form-group
            input.form-btn.btn(type='submit', value='Add')